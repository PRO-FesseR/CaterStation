import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    images: {
        domains: ['img.freepik.com',"api.microlink.io",'assets.aceternity.com','sunday.com.pk','images.unsplash.com','aceternity.com'], // Add your allowed image domains here
    },
};

export default nextConfig;
